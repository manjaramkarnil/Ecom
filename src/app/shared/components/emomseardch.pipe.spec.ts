import { EmomseardchPipe } from './emomseardch.pipe';

describe('EmomseardchPipe', () => {
  it('create an instance', () => {
    const pipe = new EmomseardchPipe();
    expect(pipe).toBeTruthy();
  });
});
